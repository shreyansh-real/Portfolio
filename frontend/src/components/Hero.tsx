import HeroFrameSequence from './HeroFrameSequence';

export default function Hero() {
  return (
    <section id="hero" style={{ position: 'relative', width: '100%', minHeight: '100vh' }}>
      <HeroFrameSequence />
    </section>
  );
}
